<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Check_Mate_Challenge.Views.MainPage"
             Title="{Binding Title}">

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <ListView x:Name="DiscovredDevices" ItemSelected="DiscovredDevices_ItemSelected"    ItemsSource="{Binding ListOfDevices}" RowHeight="50">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding name}" Font="30" TextColor="Black" FontAttributes="Bold"/>
                                <Label Text="{Binding macAddress}" Font="30" TextColor="Black" FontAttributes="Bold"/>
                            </StackLayout>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

        <Button Text="View Devices" Command ="{Binding viewCommand}" Margin="50"/>

    </StackLayout>

</ContentPage>